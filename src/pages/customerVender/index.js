import React from "react";
import List from "../list/List";
import { useSelector } from "react-redux";

const CustomerVender = () => {
  return (
    <React.Fragment>
      <List />
    </React.Fragment>
  );
};

export default CustomerVender;
